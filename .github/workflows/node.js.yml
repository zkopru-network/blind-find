name: nodejs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x, 14.x]

    steps:
    - uses: actions/checkout@v2

    - name: Cache rust and zkutil
      id: cache-zkutil
      uses: actions/cache@v2
      with:
        path: ~/.cargo/bin/zkutil
        key: ${{ runner.os }}-zkutil-${{ hashFiles('./scripts/installZkutil.sh') }}

    # https://docs.github.com/en/free-pro-team@latest/actions/reference/workflow-commands-for-github-actions#adding-a-system-path
    - name: Prepend cargo's bin directory to PATH
      run: echo "~/.cargo/bin" >> $GITHUB_PATH

    - name: Install rust and zkutil
      if: steps.cache-zkutil.outputs.cache-hit != 'true'
      run: ./scripts/installZkutil.sh

    - name: Test default cargo
      run: ls ~/.cargo/bin

    - name: Run zkutil
      run: cargo --help

    # - name: Use Node.js ${{ matrix.node-version }}
    #   uses: actions/setup-node@v1
    #   with:
    #     node-version: ${{ matrix.node-version }}

    # - run: npm ci
    # - run: npm run lint
    # # TODO: Add cache for built circuits?
    # - run: npm run build_circuits
    # - run: npm test
